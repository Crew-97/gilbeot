// 해소 #35 (2026-08-13): 화면의 Mock 표시를 전면 제거했다
//
// 이 파일은 제거 과도기용이다. 5개 화면이 아직 import 하고 있어서 지금 삭제하면
// 빌드가 깨진다. 그래서 시그니처는 그대로 두고 렌더만 멈춘다.
// 각 화면 담당이 import 와 사용처를 걷어내고 전원 머지한 뒤 이 파일을 삭제한다.
//
// Mock 경계 자체가 폐기된 것은 아니다. 어디까지가 Mock 인지는
// AGENTS.md 의 `데이터 출처` 절과 data/README.md, 결과보고서로 기록한다.

export function MockBadge() {
  return null
}
